{#page for skill panel#}
{% extends 'base.html.twig' %}

{% block body %}

    <h1>Panneau des matières</h1>
<nav class="navigation">

<a class="buttonnav" href="{{ path('admin') }}"> retour au panneau des utilisateurs</a>
    <a class="buttonnav" href="{{ path('create_skill') }}">entrer une nouvelle matière</a>

    </nav>
    <table>
        <tr>
            <th></th>
            <th></th>
            <th>id</th>
            <th>Matières</th>
        </tr>
        {% for i in skill %}
            <tr>
                <td><a href="{{ path('delete_skill',{'id': i.id }) }}"> suprimer </a></td>
                <td><a href="{{ path('update_skill',{'id': i.id }) }}"> modifier</a></td>
                <td>{{ i.id }}</td>
                <td>{{ i.skill }}



        {% endfor %}
    </table>
{% endblock %}




