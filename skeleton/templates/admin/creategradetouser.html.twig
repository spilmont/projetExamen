{% extends 'base.html.twig' %}



{% block body %}
    <h1> enregistrement des notes de {{ users.lastname }} {{ users.firstname }} de la classe de {{ users.class }}</h1>

    <a class="buttonnav" href="{{ path('admin') }}"> retour au panneau des utilisateurs</a>


    <div class="formgrade" >
    <form method="post" action="">
        {% for skill in skills if skill.class == app.user.class%}
            <label  for="grade{{ skill.id }}">{{ skill }} </label>
            <input  class="fieldgrade" type="number" name="grade{{ skill.id }}" id="grade{{ skill.id }}" min="0" max="20"><br>
        {% endfor %}
        <input type="submit">
    </form>
    </div>
    <div>

    </div>
{% endblock %}