{%  extends 'base.html.twig' %}

{% block body %}
<h1>Mot de passe oublié ?</h1>
    <a class="buttonnav" href="{{ path('homepage') }}"> retour a l'accueil</a>
<p class="margin left">Demandez à réinitialiser votre mot de passe en renseignant votre mail.</p>

<div class="formulaire"> {{ form(reset) }}</div>
    {% for message in app.flashes('error') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
    {% endfor %}
{% endblock %}